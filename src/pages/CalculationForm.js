import React, { PureComponent } from 'react';
import { connect } from 'dva';
import { 
    Card,
    Button,
    Form,
    Icon,
    Col,
    Row,
    DatePicker,
    TimePicker,
    Input,
    Select,
    Popover, 
} from 'antd';
import { connect } from 'dva';
import styles from './CalculationForm.less'

class CalculationForm extends PureComponent {
    render() {
        return (
            <div/>
        )
    }
}

export default connect(({ }) => ({
}))(CalculationForm);
